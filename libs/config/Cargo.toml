[package]
name = "hemtt-config"
version = "0.10.0"
edition = "2021"

[dependencies]
hemtt-error = { path = "../error" }

hemtt-tokens = { path = "../tokens" }

byteorder = "1.4.3"

[dev-dependencies]
hemtt-preprocessor = { path = "../preprocessor" }
