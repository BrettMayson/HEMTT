[package]
name = "hemtt-config"
version = "1.0.0"
edition = "2021"
description = "A config parser library for hemtt"
license = "GPL-2.0"

[lib]
bench = false

[dependencies]
hemtt-error = { path = "../error", version = "1.0.0" }

hemtt-io = { path = "../io", version = "1.0.0" }

ariadne = "0.3.0"
byteorder = "1.4.3"
chumsky = "0.9.2"
serde = { workspace = true }

[dev-dependencies]
hemtt-preprocessor = { path = "../preprocessor" }
