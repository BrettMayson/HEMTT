[package]
name = "hemtt-version"
version = "1.0.0"
edition = "2021"
description = "A version library for hemtt"
license = "GPL-2.0"

[dependencies]
hemtt-error = { path = "../error", version = "1.0.0" }

serde = { workspace = true }
