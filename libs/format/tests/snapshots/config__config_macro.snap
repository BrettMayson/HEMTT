---
source: libs/format/tests/config.rs
expression: config(stringify! (macro))
---
#define VALUE {1,2,3}
#define QUOTE(x) #x

class MyClass {
    requiredAddons[] = {QUOTE(ADDON), "ace_explosives"};
    value = VALUE;
    string = QUOTE(VALUE);
    author = ECSTRING(common,ACETeam);
};
